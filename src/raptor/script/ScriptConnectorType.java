package raptor.script;

/**
 * An enum for the script connector type. In the future as other connectors are
 * written they will be added here.
 */
public enum ScriptConnectorType {
	/**
	 * The ICS script connector type. Both BICS and FICS use this.
	 */
	ICS
}
